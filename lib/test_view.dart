import 'package:flutter/material.dart';

class TesdtView extends StatelessWidget {
  const TesdtView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Test View"),
    );
  }
}
